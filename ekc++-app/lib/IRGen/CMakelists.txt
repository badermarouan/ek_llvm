add_library (IRGen SHARED)

target_include_directories(IRGen
    PUBLIC
    ${CMAKE_SOURCE_DIR}/ekc++-app/include/IRGen
)

target_link_libraries(IRGen
    PUBLIC
        LLVM
)

target_sources(IRGen
    PRIVATE
        IRGen.cpp
)
